name: test job
on: push
jobs:
  test_job:  # Adding an identifier for the job
    runs-on: ubuntu-latest  # Corrected `runs_on` to `runs-on`
    steps:
      - name: null
        run: echo "${{ secrets.good }}"  # Closed the double quotes properly
